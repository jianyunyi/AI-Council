'use client'
import {useState} from 'react'
export function ApprovalPanel({hash,version,onApprove}:{hash:string;version:number;onApprove:()=>Promise<void>|void}){const [confirmed,setConfirmed]=useState(false);const [busy,setBusy]=useState(false);async function approve(){if(!confirmed)return;setBusy(true);try{await onApprove()}finally{setBusy(false)}}return <section className="card"><h2>人工批准执行</h2><p>计划版本：{version}</p><code>{hash||'等待计划哈希'}</code><p>批准前请检查所有变更文件、命令参数、工作目录、超时和恢复策略。</p><label><input type="checkbox" id="confirm" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)}/> 我确认这是本次要执行的不可变计划</label><br/><button onClick={approve} disabled={!confirmed||busy}>{busy?'执行中…':'批准并执行'}</button></section>}
