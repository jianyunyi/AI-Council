'use client'
import {FormEvent, useState} from 'react'
export default function NewTask(){const [requirement,setRequirement]=useState('');const [acceptance,setAcceptance]=useState('');function submit(e:FormEvent){e.preventDefault();if(!requirement.trim()||!acceptance.trim())return;alert('任务已提交（API 接入待配置）')}return <><h1>新建任务</h1><form className="card" onSubmit={submit}><label>需求<textarea required value={requirement} onChange={e=>setRequirement(e.target.value)} /></label><label>验收标准<textarea required value={acceptance} onChange={e=>setAcceptance(e.target.value)} placeholder="每行一条" /></label><label>Quorum<input type="number" min="1" max="9" defaultValue="2" /></label><label>最大轮次<input type="number" min="1" max="5" defaultValue="2" /></label><button type="submit">开始协作分析</button></form></>}
